apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx
    Version: 1.3.2
spec:
  containers:
  - name: nginx-container
    image: nginx:latest
    ports:
    - containerPort: 9080
